# CLAUDE.md

Momento 프로젝트 작업 규칙. 모든 작업은 이 문서를 기준으로 수행한다.

---

## 1. 프로젝트 개요

Momento는 가족·친구·연인·아이 성장의 사진과 영상, 추억을 함께 모아 하나의 살아있는 앨범을 만드는 서비스다.

- 사진을 **저장**하는 서비스가 아니라 함께 **추억을 만드는** 서비스다.
- **결과물은 한 권의 앨범이고, 함께 쓰는 즐거움이 사람을 다시 오게 한다.**
  한마디·"우리가 남긴 말"·반응은 그 즐거움을 위해 넣은 것이다. 있어야 앨범이 산다.
  이런 요소를 "SNS 같다"는 이유로 빼지 않는다.
  **다만 모든 사회적 기능은 하나의 앨범 안에서 일어난다.**
  알고리즘 추천·팔로우·공개 탐색처럼 **앨범 밖으로 사람을 끌고 나가는 것은 만들지 않는다.**
  **관계는 끝나지 않고, 앨범은 완성된다.**
  **내가 속한 관계 안에서 일어난 일은 알려준다 — 그것이 다시 찾아올 이유다.**
- AI는 내부 구현에만 사용한다. 사용자에게 AI를 노출하지 않는다.
- **얼굴 인식은 기기 안에서만 수행한다.** 얼굴 특징 데이터를 서버로 전송하거나 저장하지 않는다.
  성능 선택이 아니라 사용자에게 한 약속이다. (§9 참고)

모든 기능은 다음 기준으로 판단한다:
> "사용자가 더 쉽게 추억을 만들고, 함께 참여하고, 다시 찾아오게 만드는가?"

현재 상태: **MVP**. 빠르게 출시하고 실사용자 피드백으로 개선하는 것이 목표다.

---

## 2. 기술 스택 / 구조

| 영역 | 스택 | 배포 |
| --- | --- | --- |
| Frontend | React 19 + Vite + TypeScript | Vercel (`https://momento-ashen-rho.vercel.app`) |
| Backend | FastAPI (Python) | Railway |
| DB / Storage | Supabase (Postgres + Storage) | - |
| AI | OpenAI `gpt-4o-mini` (내부 전용) | - |

```
D:\Momento
├── backend/          FastAPI (app/, tests/, prompts/, scripts/)
├── frontend/         React + Vite (src/, tests/, api/, scripts/)
├── supabase/         schema, migrations
├── docs/             제품·운영 문서
└── .agents/          (구) Codex 규칙 파일
```

API 연결 방식:
- 로컬: `VITE_API_BASE_URL=http://localhost:8000` → 백엔드 직접 호출
- 프로덕션: `VITE_API_BASE_URL` 비움 → 같은 origin `/api/*` → Vercel 서버리스 프록시 → `MOMENTO_API_URL`

---

## 3. 명령어

작업 후 아래를 **반드시** 실행한다.

### Frontend (`cd frontend`)
```bash
npm run dev            # 개발 서버 (localhost:5173)
npm run test:frontend  # 프런트 테스트 전체
npm run build          # tsc -b && vite build  ← 필수
```

### Backend (`cd backend`)
```bash
python -m pytest -q         # 백엔드 테스트  ← 필수
python -m compileall -q app # 컴파일 체크
uvicorn app.main:app --reload --port 8000
```

### 공통
```bash
git diff --check
```

새 테스트를 추가하면 `frontend/package.json`의 `test:frontend` 스크립트에 파일을 등록한다.

---

## 4. 개발 원칙

### MVP 우선
- 항상 MVP를 우선한다. 새 기능보다 **버그 수정 → UX 개선 → 안정성 → 출시** 순.
- 완벽한 아키텍처보다 동작하는 소프트웨어가 먼저다.
- 큰 리팩터링은 하지 않는다.

### 최소 수정
- 요청한 내용만 수정한다. 요청하지 않은 기능은 절대 건드리지 않는다.
- 수정 파일 수를 최소화한다. 기존 구조·아키텍처를 유지한다.
- 기존 컴포넌트를 재사용한다. 중복 코드를 만들지 않는다.
- 동작하는 코드를 다시 쓰지 않는다.
- 새 라이브러리는 꼭 필요한 경우가 아니면 추가하지 않는다.

### 금지사항 (사용자가 명시적으로 요청한 경우에만 수행)
- 임의 리팩터링 / 컴포넌트 분리 / 파일 구조 변경
- 라우팅 변경 / API 구조 변경 / DB 구조 변경
- 디자인 전면 수정 / 사용자 경험을 바꾸는 임의의 변경
- 보고 없이 기존 정책 변경

### 보고 없이 진행
작업 중간에 "이렇게 할까요?", "어느 쪽이 좋을까요?" 같은 질문을 하지 않는다.
명확한 요구사항이면 바로 구현한다. 선택지가 여러 개면 스스로 고른다:
1. 가장 작은 구현
2. 가장 위험이 낮은 구현
3. 기존 코드를 가장 많이 보존하는 구현

**질문이 허용되는 경우 (불가능하거나 위험할 때만)**
- 저장소 접근 불가 / 필수 API 키·시크릿 없음 / 외부 서비스 사용 불가
- 필수 정보를 추론할 수 없음
- 리팩터링·구조 변경·UX 변경·DB 변경이 필요하다고 판단될 때 → 멋대로 하지 말고 **제안**한다

구현 스타일, 네이밍, 폴더 구성, UI 디테일, 최적화 방식은 **묻지 않고 보수적인 쪽으로 결정**한다.

### 기존 기능 우선
- 새 기능 때문에 기존 기능을 깨뜨리지 않는다.
- 회귀 테스트를 반드시 수행하고, 필요하면 추가한다.

---

## 5. 사용자 플로우 (변경 금지)

```
사진 업로드 → 사진별 코멘트 입력 → 앨범 자동 생성 → 결과 확인
→ 사용자 자유 수정 → 공유 / PDF
```

- "AI가 만들어주기" 같은 버튼은 만들지 않는다. 업로드 후 자동 생성된다.
- 사용자는 생성된 결과를 **수정만** 한다.

---

## 6. 앨범 구조 (본문 변경 금지)

```
월
 └ 날짜 (YYYY.MM.DD + 사진 수)
    └ 사진
       └ 사진 캡션
    └ "YYYY.MM.DD의 이야기"
 └ 다음 날짜 …
"우리의 이야기"
 └ "함께 만든 사람들" (이름 한 줄)                     ← PDF 포함
 └ 반응 (❤️ 좋아요 / 🥹 뭉클해요 / 😊 웃음이 나요)   ← 웹/공유만, PDF 제외
 └ "우리가 남긴 말" (앨범 전체에 남기는 짧은 글)      ← 웹/공유만, PDF 제외
```

- 앨범 **본문**(월→날짜→사진→캡션→날짜 이야기→"우리의 이야기") 구조는 변경 금지.
- **"함께 만든 사람들"** — "우리의 이야기" 바로 다음에 참여자 이름을 한 줄로 넣는다.
  캡션에 이름을 넣지 않는 대신, 여러 사람이 함께 만들었다는 사실이 **인쇄물에 남는 자리**다.
  예: `함께 만든 사람 — 곽병준 · 영희 · 준3`
- 반응과 **"우리가 남긴 말"**은 그 다음에 붙는 참여 요소다(웹·공유 화면 전용).
  **PDF(print)에는 넣지 않는다.** (`docs/PARTICIPATION_DESIGN.md` §2·§3)
- **"방명록"이라는 이름은 쓰지 않는다.** 방문한 사람이 쓰는 말이라, 주최자가 자기 앨범에
  쓸 때 말이 맞지 않는다. 구역 제목은 **"우리가 남긴 말"** 하나이고, 누르는 버튼은
  **"한마디 남기기"**다. 역할에 따라 이름을 다르게 부르지 않는다.

제거된 요소 (되살리지 않는다): `Day 1` / `Day 2` / `여행의 시작` / `우리의 추억` / `행복한 순간` / 그룹 제목 / 월별 이야기

### 사진 캡션
사진이 가장 중요하다. 긴 텍스트보다 사진을 크게 보여준다.
- 사진 아래 배치, 최대 2줄, 넘치면 `…`
- 카드 / 테두리 / 그림자 / 배경색 **금지**
- **작성자 이름을 넣지 않는다.** 캡션은 그 사진을 올린 사람의 말이므로 이름이 없어도
  누구 말인지 자연스럽고, 사진마다 이름이 붙으면 인쇄물이 지저분해진다.
  누가 썼는지는 프레임 **밖** 한마디에서 보인다.
- 쓸 수 있는 사람은 **그 사진을 올린 사람**뿐이다. 주최자는 남이 올린 사진의 캡션도
  고칠 수 있다(인쇄되기 때문). 자세한 권한은 `docs/SCREEN_SPEC.md` §7.

### 날짜 이야기
- 각 날짜 마지막에 "YYYY.MM.DD의 이야기"를 배치한다.
- 사진·캡션·촬영일만을 근거로 작성한다. 추측하지 않는다. 사실 기반.
- 3~6줄 유지.

---

## 7. UI 원칙

사용자는 쉽고 따뜻한 경험을 해야 한다. 기술적인 느낌보다 감성적 경험이 먼저다.

- 항상 **한 단계 적게, 한 번 덜 클릭하게** 생각한다.
- 버튼을 하나 더 만드는 것보다 기존 화면에서 해결하는 것을 우선한다.
- 새 페이지보다 **Drawer / Bottom Sheet / Modal / Inline Panel**을 우선 사용한다.
- 기존 레이아웃을 유지한다. 요청 없이 화면을 재디자인하지 않는다.
- 모바일 우선(카카오톡 웹뷰) 동작을 유지한다.
- 불필요한 애니메이션·시각 효과를 넣지 않는다.

우선순위: **사용성 > 속도 > 감성적 경험 > 시각적 완성도**. 미관을 위해 사용성을 희생하지 않는다.

---

## 8. 카피라이팅

다음 표현을 **절대 사용하지 않는다**: `AI`, `GPT`, `인공지능이 만들었습니다`

사용자는 "Momento가 만들어준 경험"으로 느껴야 한다.
기술 용어를 피하고, 사용자 관점에서, 따뜻하고 자연스러운 말로 쓴다.

---

## 9. 데이터 / 사진 / 성능

### 사진
- 업로드 시 클라이언트가 **긴 변 2560px 로 축소**한다(`frontend/src/lib/optimizeImageFile.ts` 의 `MAX_EDGE`).
  이는 **의도된 정책**이며 A4 300DPI 인쇄까지 커버한다. `MAX_EDGE` 를 임의로 바꾸지 않는다
  (올리면 업로드 부담만 커진다). 따라서 저장되는 "원본"은 이 2560px 상한 파일이다.
- **업로드된 원본(2560px 상한)은 항상 보존한다.** 삭제·손실 금지.
- 화면 표시는 display(WebP)를 우선 사용한다.
- 다운로드·인쇄는 (업로드) 원본을 사용한다.
- 이미 로드한 이미지는 다시 다운로드하지 않는다. Display Asset을 적극 활용한다.

### 얼굴 인식 / 인물 검색 (온디바이스 원칙)
- **얼굴 검출·인코딩·매칭은 전부 사용자 기기 안에서 수행한다.** 서버로 보내지 않는다.
- **얼굴 특징 벡터(임베딩)를 서버에 전송하거나 저장하지 않는다.** 사진 자체와 달리 얼굴 벡터는
  생체정보에 준해 다룬다.
- 서버로 올라가는 것은 **사용자가 고른 결과 사진**뿐이다. 후보군·비선택 사진·얼굴 데이터는 올라가지 않는다.
- 이 원칙은 아프신 분·돌아가신 분의 사진이라는 맥락에서 특히 중요하다. 신뢰가 한 번 깨지면 회복되지 않는다.
- 관련 구현: 별도 저장소 `Momento-photo-selector` (Windows 데스크톱). 향후 네이티브 앱도 동일 원칙을 따른다.

### 데이터
- 사용자 데이터는 절대 잃어버리면 안 된다.
- 삭제는 항상 안전하게 수행한다. Storage와 DB 정합성을 고려한다.

### 성능
- 앨범 전체를 다시 렌더링하지 않는다. 가능하면 부분 업데이트를 사용한다.
- `AlbumRenderer`는 가능하면 재마운트하지 않는다.
- 웹 / 공유 / PDF는 **모두 동일한 `AlbumRenderer`**를 사용한다. 레이아웃만 다를 수 있고 내용은 동일해야 한다.

### Vision
- Vision 분석 결과가 있으면 재분석하지 않는다. 기존 결과를 재사용해 비용을 최소화한다.

---

## 10. 권한 / API

- `Owner` / `Contributor` / `Visitor` 권한을 명확히 구분한다.
- **권한 검사는 프런트가 아니라 백엔드에서 수행한다.**
- 기존 API를 깨지 않는다. 기존 계약을 최대한 유지한다.
- 새 API를 만들기보다 기존 API 확장을 우선한다.

---

## 11. Git / 배포

### 모든 수정 (2026-08-23 — 환경을 운영 하나로)

**`main` 에서 한다.** 갈래를 나누지 않는다.

```
구현 → 검사 전체(frontend test · backend test · build) → 커밋 → main 푸시
→ 아래 배포 검증 4가지 확인 → 실기기 확인
```

`dev` 브랜치와 Railway `dev` 환경은 **당분간 쓰지 않는다.** 지우지는 않았다 —
되돌아갈 자리로 남겨 둔다. 다시 쓰기로 하면 `docs/ENVIRONMENTS.md` 에 그때의 설정이
그대로 적혀 있다.

미완성 UI 변경을 프로덕션에 바로 배포하지 않는다.

### ★ 되돌릴 수 있는 것과 없는 것을 가른다

개발 환경이라는 안전망을 접었으므로, 그 자리를 이 규칙이 대신한다.

| | 되돌리기 |
| --- | --- |
| 코드 | `git revert` 로 되돌아온다. **운영에서 고쳐도 된다.** |
| 운영 데이터 | **되돌아오지 않는다.** 아래 둘을 반드시 지킨다. |

**1) migration 은 `시험 → 되돌리기 → 적용` 순서다.**

`DO` 블록 안에서 임시 자료를 만들고, 함수를 돌리고, 결과를 검사하고, 마지막에
`RAISE` 로 전체를 되돌린다. **통과한 것만 실제로 건다.**
(2026-08-22 `merge_profiles` 에서 이 방식으로 데이터 손실을 잡았다)

**2) 사람을 가둘 수 있는 변경은 되돌리는 방법을 먼저 정하고 올린다.**

로그인·약관·권한처럼 **막히면 아무것도 못 하는** 것이 여기 해당한다.
되돌릴 방법이 없으면 **올리지 않는다.**
(2026-08-10 K-14 약관 동의가 프로덕션의 모든 앨범을 막았다)

★ 실사용자가 생기면 이 결정을 다시 본다. 지금은 사용자가 PO 뿐이라 성립한다.

### 배포 검증 (중요)
`git push` 성공은 프로덕션 반영을 의미하지 않는다. 프로덕션 작업은 아래 4가지를 모두 확인하기 전까지 "배포 완료"로 보고하지 않는다.
1. `origin/main` 커밋 해시
2. Vercel production 배포 커밋 해시
3. production alias가 최신 배포를 가리키는지
4. 실제 프로덕션 URL에 변경이 반영됐는지

---

## 12. 완료 보고 형식

작업이 끝나면 아래 형식으로만 보고한다. 불필요한 설명은 하지 않는다.

```
원인:
수정 내용:
변경 파일:
영향받은 기능:
테스트 결과:   (frontend test / backend test / build)
Migration 필요 여부:
커밋 해시 / 푸시 결과:
```

---

## 13. 코딩 스타일

- 명확한 이름을 사용한다.
- 매직 넘버를 만들지 않는다.
- 공통 함수가 있으면 재사용한다.
- 중복 코드를 만들지 않는다.

---

## 14. 참고 문서 (`docs/`)

| 문서 | 용도 |
| --- | --- |
| `PRODUCT_BACKLOG.md` | 무엇을 만들지 / 현재 구현 상태 |
| `LAUNCH_CHECKLIST.md` | 실제 배포 환경 설정 완료 여부 |
| `KNOWN_ISSUES.md` | 의도된 제약 사항 |
| `BUG_LOG.md` | 재현 가능한 결함 |
| `OPERATIONS.md` | 운영 로그 / health check / cleanup |
| `SOCIAL_AUTH_SETUP.md` | 카카오·네이버 OAuth 설정 방법 |
| `STORAGE_ARCHITECTURE.md` | Storage 구조 |
| `DATABASE_PLAN.md` | DB 설계 |
| `CODEBASE_RISK_AUDIT.md` | 리스크 감사 결과 |
| `DB_NOTES.md` | 남은 옛 설계 — **기록이다. 고치라는 목록이 아니다** |
| `DATA_CHECKS.md` | 데이터가 규칙과 맞는지 보는 조회 모음 |

문서와 코드가 다르면 **코드를 신뢰**하고, 문서를 갱신한다.
