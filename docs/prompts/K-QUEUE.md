# K그룹 작업 큐 — 데이터가 0인 지금만 할 수 있는 것

작성 2026-08-09. **앨범·사진·참여자가 전부 0건인 상태에서 시작한다.**

**규칙** — J-QUEUE 와 같다.

1. **위에서 아래로 한 건씩.** 한 건이 끝나면 커밋·푸시·보고하고 **멈춘다.**
2. 시작 전에 `docs/SCREEN_SPEC.md`(**19차**)를 읽는다. 보고에 **§몇**을 적는다.
3. 문서와 다르게 해야 한다고 판단되면 고치지 말고 **보고**한다.
4. 제목 옆에 `[완료 <커밋해시>]` 를 적는다.
5. ★ **브라우저에서 치수를 재지 않는다.** 값은 큐가 준다. (시간·개수는 재도 된다)
6. ★ 항목 크기 — **작음**(테스트 없음) / **보통**(테스트 한 건) / **큼**(조사 먼저)

---

## ★★ 이 큐를 하는 동안 지킬 것

- ★ **앨범을 만들지 않는다.** PO 도 만들지 않는다.
  중간 상태에서 만든 앨범은 옛 버킷·옛 키에 남아 창이 닫힌다.
- ★ **K-1 은 세 커밋으로 나눈다.** 한 번에 몰지 않는다. 깨지면 어디서 깨졌는지 알아야 한다.
- ★ **되돌리기 어려운 작업이다.** 각 커밋 뒤에 실제로 앨범 하나를 만들어 보고
  사진 업로드 → PDF → 카카오 로그인까지 확인하고 다음으로 간다.
  (확인용 앨범은 다음 커밋 전에 지운다)

---

## K-1. 🔴 `Momento` 이름을 전부 바꾼다 (J-14 전면판) — **크기: 큼**

데이터가 0건이라 지금은 **바꿔도 아무도 잃지 않는다.**
사진이 한 장이라도 쌓이면 버킷은 **영원히 못 바꾼다.**

### K-1-a. DB 트리거 기본 이름 + 개발자 문자열

- `Momento 사용자` → **`우리앨범 사용자`**
  네 개 migration 에 흩어져 있다. **새 migration 하나로 덮는다.** 옛 파일은 고치지 않는다.
- `console.debug("[Momento] …")` · `console.warn` 15곳 → `[우리앨범]`
- 주석의 `Momento` (예: `tokens.css` 의 `Momento 디자인 토큰`)

### K-1-b. localStorage 키 + HTTP 헤더

```
momento-*  · momento_*   →  woorialbum-* · woorialbum_*
X-Momento-*              →  X-Woorialbum-*
```

- ★ **빠짐없이 한 커밋에서.** 프런트와 백엔드가 같은 커밋이어야 한다(§11).
  하나라도 남으면 요청이 조용히 실패한다.
- ★ **옛 키를 읽어 옮기는 코드를 만들지 않는다.** 남은 사용자가 없다.
  그런 코드는 영원히 안 지워진다.
- 배포 사이 몇 분간 요청이 깨질 수 있다. **그 시간에 아무도 쓰지 않는다.**

### K-1-c. Storage 버킷 — **마지막에 따로**

- 새 버킷 **`woorialbum-private`** (비공개) 생성
- Storage 접근 정책을 옛 버킷과 **같게** 만든다
- Railway 환경변수 **`SUPABASE_STORAGE_BUCKET`** 교체
- 옛 버킷 **둘 다** 삭제 — `momento-private`(파일 218개, 전부 고아) · `albums`(빈 껍데기)
- ★ 코드에 버킷 이름이 박힌 곳이 있는지 확인한다. 기본값
  (`config.py:20 supabase_private_storage_bucket`)도 새 이름으로.

### 회귀 테스트

- ★ 소스 어디에도 `momento` 가 남지 않을 것 (대소문자 무시, 주석 포함)
  단 옛 migration 파일은 예외 — **이력이라 고치지 않는다**
- 새 계정의 기본 표시 이름이 `우리앨범 사용자` 일 것

### 끝나고 확인 (PO)

앨범 하나 만들기 → 사진 5장 → 캡션 → PDF → 카카오 로그아웃/로그인 → 앨범 지우기


---

## K-6. 🔴 PDF 를 못 받는다 [완료 5fb205d] — **크기: 큼**

K-1-c 배포 뒤 새로 만든 앨범에서 PDF 를 받으려니 오류가 난다.

### 확인된 사실 (PO 쪽에서 DB·Storage 를 직접 읽음)

앨범 `ff3c6e02-5e96-4362-bf77-d2bf4a376664` · 사진 5장.
`woorialbum-private` 에 들어간 객체 **16개**:

```
albums/<id>/results/<uuid>.png              1개  ← 표지 이미지
albums/<id>/photos/<photoId>/display.webp   5개
albums/<id>/photos/<photoId>/thumbnail.webp 5개
albums/<id>/photos/<photoId>/<원본>         5개
```

★ **PDF 객체가 하나도 없다.** 받는 단계가 아니라 **만들어 올리는 단계**에서
끊겼을 가능성이 크다.

`album_photos.storage_bucket` 은 전부 `woorialbum-private` 이고 다른 값이 0이다 —
사진 경로는 정상이다.

### ★ PO 답 (2026-08-09) — 초대장 화면(JoinPage)이다

초대 링크를 눌러 들어간 **초대장 화면**에서 `카카오로 시작하기` 를 눌렀는데
아무 일도 없었다. **이름을 넣으니 참여는 됐다.**

→ **참여 자체는 막히지 않는다.** 로그인 경로만 죽어 있다.
   급한 결함은 아니지만, 그 자리에서 로그인하면 참여가 계정에 바로 붙어
   `함께 만드는 앨범` 에 남는다. 안 되면 게스트로 남는다.

### ★ 원인을 밝힌 뒤 갈림길이 있다

카카오톡 웹뷰가 `accounts.kakao.com` 으로 나가는 것을 **막는 것이라면 우리가 고칠 수 없다.**
그때는 고치지 말고 **그 버튼을 없앤다.**

- ★ **눌러도 안 되는 버튼을 두는 것이 §4 가 가장 나쁘다고 한 것**이다.
- 잃는 것이 거의 없다 — 이름을 넣고 들어가도 나중에 로그인하면 참여가 계정에 붙는
  길이 이미 있다(`woorialbum-guest-pending-claim`).
- 고칠 수 있으면 고친다. **먼저 원인을 밝히고 어느 쪽인지 보고한다.**

### ★ 먼저 확인해서 보고할 것

- **어디서 끊기는가?** 브라우저에서 PDF 를 만드는가 → 서버에 올리는가(`PUT /albums/{id}/pdf`)
  → 주소를 받아 여는가. 셋 중 어디인지 **로그(`[pdf] event=…`)로 특정**한다.
- ★ **K-1-c 가 원인인지 먼저 가른다.** 버킷 이름을 옛 값으로 되돌린 상태에서
  같은 일이 되는지 본다. 되면 K-1-c 가 원인이고, 안 되면 별개다.
  (되돌려 확인만 하고 커밋하지 않는다)
### 화면에 뜬 문구 (PO 스크린샷 확인함)

> 카카오톡 안에서는 파일 저장이 막혀 있어요. 메뉴(⋯)에서 '다른 브라우저로 열기'를
> 눌러 크롬이나 사파리에서 저장해 주세요.

★ **우리 문구다.** G-2 에서 *"모든 전달 경로가 실패했을 때만 나오는 마지막 안내"*
라고 못박은 바로 그것이다.

사슬이 맞아떨어진다:

```
PDF 를 저장소에 올린다    →  실패 (버킷에 PDF 객체 0개)
그래서 https 주소가 없다  →  blob 으로 대체
카카오톡이 blob 을 막는다 →  이 안내가 뜬다
```

**G-2(`40ed064`)와 같은 자리다.** 그때 원인은 `albumVersion: 0` → 409 였다.
이번엔 다른 이유일 수 있다 — **단정하지 말고 로그(`[pdf] event=…`)로 특정한다.**

### ★ PO 답 (2026-08-09) — `파일로 저장하기(PDF)` 를 눌렀을 때다

카카오 공유가 아니라 **PDF 다운로드**에서 난 것이 확인됐다. 사슬이 그대로 맞다.

PO 물음: *"카카오톡에서 PDF 다운로드하면 바로 안 돼? 원래는 됐던 것 같은데"*
→ **원래 됐다.** G-2(`40ed064`)가 고친 정상 흐름은 이것이다.

```
PDF 를 만든다 → 저장소에 올린다 → https 주소를 받는다 → 카카오톡이 그 주소로 받는다
```

카카오톡 웹뷰는 **blob 은 막지만 https 주소는 받는다.** 그래서 올려서 주소로 준다.
지금은 409 로 **올리는 단계가 통째로 막혀** blob 으로 떨어진다.
**409 를 고치면 다시 바로 받아진다.** 안내 문구를 고쳐서 될 일이 아니다.

### ★ G-2 의 회귀 테스트가 왜 못 잡았는지도 본다

G-QUEUE 는 *"회귀 테스트: 업로드→주소 전달 경로가 끊기면 실패할 것"* 을 요구했고
그 테스트가 들어갔다. **그런데 못 잡았다.**

- 그 테스트가 **실제 경로를 보고 있는가?** 아니면 그 근처만 보고 있는가.
- 못 잡은 이유를 고치는 것이 이번 수정의 절반이다.
  같은 자리가 세 번째로 깨지지 않게 한다.
- `albums.result_bucket` · PDF 캐시 키(`{version}:r{renderer_version}`)가
  새 버킷을 가리키는지 확인한다.

### 고칠 것 — PO 승인 (2026-08-09)

- **서버의 409 검사는 그대로 둔다.** 옳은 검사다 — 화면이 낡았으면 PDF 도 낡는다.
- **화면이 저장 응답의 버전을 받아 둔다.**
- ★ **버전을 올리는 곳이 넷이다** — 제목 · 날짜 이야기 · 캡션 · `우리의 이야기`.
  화면이 받아 두는 곳은 그보다 적다. **하나라도 빠지면 같은 결함이 다시 난다.**

### 회귀 테스트

- ★ **네 경로 각각을 잠근다.** 제목 · 이야기 · 캡션 · `우리의 이야기` —
  **각각 저장한 뒤 PDF 요청의 버전이 서버 값과 같은지** 본다.
  하나만 잠그면 나머지 셋이 다음에 깨진다.
- ★ **G-2 의 테스트가 이번에 못 잡은 이유**를 찾아 보고에 적는다.
  같은 자리가 세 번째로 깨지지 않게 한다.

### 지킬 것

- ★ 카카오 웹뷰에서 **주소로 여는 경로**(G-2)를 깨지 않는다.
- ★ I-3 에서 만든 진행·결과 표시를 깨지 않는다.

---

## K-7. 🔴 참여한 사람이 `카카오로 시작하기` 를 눌러도 아무 일이 없다 — **크기: 큼**

초대 링크로 들어와 이름을 넣고 참여하면 앨범이 열린다. 거기서 `카카오로 시작하기`
를 누르면 **그 자리에 그대로 있다.** 아무 일도 일어나지 않는다.

### ★ 먼저 확인해서 보고할 것

- 누를 때 **무슨 일이 일어나는가?** 요청이 나가는가, 나가다 막히는가,
  아예 아무것도 안 하는가. 콘솔 오류가 있는가.
- ★ **K-1-b 가 원인인지 먼저 가른다.** 그 커밋에서
  `momento-auth-return-to` → `woorialbum-auth-return-to` 로 바꿨다.
  로그인 뒤 돌아올 자리를 적어 두는 키다. 여기가 어긋나면 정확히 이 증상이 난다.
  ★ **다만 단정하지 않는다.** 다른 원인일 수 있다 — 확인하고 보고한다.
- 참여자(게스트)가 로그인하는 흐름과 그냥 로그인하는 흐름이 **다른 코드를 타는지** 본다.
- Supabase Auth 의 Redirect URL 목록에 지금 주소가 들어 있는지 확인한다.

### 지킬 것

- ★ **조용히 실패하지 않게 한다**(§11). 눌렀는데 아무 일도 없는 것이 지금 가장 나쁘다.
  막히는 이유가 있으면 사용자에게 말해야 한다.
- ★ 게스트가 로그인하면 **그때까지 한 참여가 이어져야 한다**(§1).
  로그인하면서 참여 기록이 끊기면 그것이 더 큰 결함이다. 함께 확인한다.

---

★ **K-6 · K-7 이 끝나기 전에는 ⑥⑦(옛 변수·옛 버킷 삭제)을 하지 않는다.**
되돌아갈 곳을 남겨 둔다.


---

## K-8. 할 일이 있는 결과는 눈에 띄어야 한다 — **크기: 보통**

PDF 를 받지 못했을 때 뜨는 안내가 **화면 맨 아래에 지나가듯 떠서 눈에 안 띈다.**
그런데 그 안내는 **사용자가 뭔가를 해야** 파일을 받을 수 있는 내용이다.

I-3 에서 PDF 표시를 하단 고정으로 둔 것은 **만드는 동안 화면을 가리지 않으려고**였다.
그건 맞았다. 문제는 **끝났고 사용자가 할 일이 있는 경우**까지 같은 자리에 둔 것이다.

### 자리 — 셋으로 나눈다

| 상태 | 자리 |
| --- | --- |
| **만드는 중** | 하단 고정 · 가리지 않음 **(지금 그대로)** |
| **끝났고 할 일 없음** | 하단 고정 · 사용자가 닫음 **(지금 그대로)** |
| **끝났는데 할 일이 있음** | ★ **화면 가운데 시트 + 딤** |

- ★ 딤과 시트는 **이미 있는 것을 쓴다** — `album-sheet-dim` · `album-inline-action`.
  새로 만들지 않는다(§11).
- 사용자가 방금 누른 행동의 결과다. **가려도 된다.** 놓치면 무엇을 해야 할지 모른다.
- 버튼은 **`확인` 하나.** 되돌릴 것이 없다.

### 문구 — 다시 쓴다

```
지금
  카카오톡 안에서는 파일 저장이 막혀 있어요. 메뉴(⋯)에서 '다른 브라우저로 열기'를
  눌러 크롬이나 사파리에서 저장해 주세요.
```

문제 셋 — `막혀 있어요`로 **나쁜 소식부터** 말하고, **어디를 눌러야 하는지** 없고,
`크롬이나 사파리`라는 이름을 대서 **그게 뭔지 찾게** 만든다.

```
바꿀 것

  앨범 파일이 준비됐어요                       ← 제목. 굵게

  카카오톡에서는 바로 저장되지 않아요.
  브라우저 메뉴(⋮ 또는 ···)에서
  '다른 브라우저로 열기'를 고르면 저장할 수 있어요.

                                    [ 확인 ]
```

- ★ **좋은 소식이 먼저다.** 파일은 실제로 만들어져 있다. 그것부터 말한다.
- ★ **`크롬`·`사파리`를 빼낸다.** `다른 브라우저로 열기`를 고르면 알아서 열린다.
  이름을 대면 40~60대는 그것을 찾아 헤맨다.
- ★ 아이콘을 **둘 다** 보여준다(`⋮` · `···`). 기기·버전마다 다르다.
- `막혀 있어요` → `바로 저장되지 않아요`. 사실은 같고 누구 탓도 아니다.

### 색

- 오류가 아니라 **안내**다. 사용자가 잘못한 것이 없다 — danger 색을 쓰지 않는다(I-5b).
- 눈에 띄는 것은 **색이 아니라 자리와 딤**이 맡는다.
- 제목만 굵게. 본문은 지금 크기.

### 지킬 것

- ★ **만드는 중 표시와 성공 표시는 건드리지 않는다**(I-3). 자리도 그대로다.
- ★ 이 안내는 **K-6 이 고쳐지면 거의 안 뜬다.** 그래도 마지막 안내라 좋아야 한다.
  네트워크가 나쁘면 언제든 이 길로 온다.

### 회귀 테스트

- ★ 할 일이 있는 결과가 **딤 위 시트**로 뜰 것
- ★ 만드는 중 표시는 여전히 **하단 고정**일 것
- 문구에 `크롬` · `사파리` · `막혀` 가 없을 것

---

## K-2. 🔴 FK 를 `CASCADE` 로 — **크기: 큼**

`albums` 를 가리키는 자식 17개 중 **8개가 `RESTRICT`** 이고 의존이 **두 겹**이다.

```
memory_answers → memory_questions → album_media → albums
guest_memory_submissions → share_links → albums
```

그래서 삭제 RPC 가 **순서를 손으로 알고 있어야 한다.**
2026-08-09 에 PO 가 앨범을 지우려다 두 번 막혔고, 나(문서 쪽)도 순서를 두 번 틀렸다.

★ **DB 가 알려주지 않으면 사람은 반드시 틀린다.**
새 자식 테이블이 하나 얹히고 RPC 에 안 들어가면, **그 순간부터 프로덕션에서
앨범 삭제가 조용히 실패한다.** 테스트로도 안 잡힌다.

### 고칠 것

- `albums` 를 가리키는 `RESTRICT` 여덟을 **`ON DELETE CASCADE`** 로.
  `album_media` · `album_members` · `album_photos` · `album_story_inputs` ·
  `guest_album_sessions` · `guest_memory_submissions` · `memory_questions` · `share_links`
- 자식들끼리의 `RESTRICT` 도 같이 — `memory_answers → memory_questions`,
  `memory_questions → album_media`, `guest_memory_submissions → share_links`,
  `photo_memories → album_contributors`
- ★ **`SET NULL` 은 그대로 둔다** — `analytics_events` · `ai_usage_logs`.
  통계는 앨범이 사라져도 남아야 한다.
- ★ **RPC 의 손 열거를 지운다.** DB 가 하는 일을 코드가 또 하지 않는다.

### ★ 확인해서 보고할 것

- 지금 RPC(`delete_album_cascade`)가 위 여덟을 **다 지우는가?** 하나라도 빠져 있으면
  그것이 이미 결함이다.
- CASCADE 로 바꾼 뒤 **RPC 가 하는 일이 남아 있는가?** 없으면 RPC 를 지울지 보고한다.
  (권한 검사는 API 가 이미 한다)

### 회귀 테스트

- ★ 앨범 하나를 지우면 **자식이 전부 사라질 것** — 여덟 테이블 모두
- ★ `analytics_events` · `ai_usage_logs` 는 **남고 `album_id` 만 비워질 것**
- ★ 코드에 삭제 순서를 손으로 적은 곳이 없을 것

---

## K-3. Storage 파일이 DB 와 따로 논다 — **크기: 큼**

삭제 RPC 주석에 이렇게 적혀 있다.

> Storage objects are deliberately not handled here: the API collects their
> paths before this transaction, then removes them as a best-effort cleanup.

그래서 **API 를 타지 않는 삭제는 파일을 남긴다.** 주인 없는 게스트 앨범이 정확히 그 경우다.
2026-08-09 현재 옛 버킷에 **고아 파일 218개**가 있다(K-1-c 에서 버킷째 지운다).

### ★ 먼저 확인해서 보고할 것

- Storage 와 DB 를 **한 트랜잭션으로 묶을 수 있는가?**
  ★ 아마 못 묶는다(Storage 는 별도 서비스다). 못 묶으면 **묶으려 하지 말고**
  "지우고 남으면 나중에 줍는다"로 간다. 그게 현실적이다.

### 고칠 것

- **고아 파일을 찾는 조회**를 `docs/DATA_CHECKS.md` 에 넣는다
  (`storage.objects` 에는 있는데 `album_photos` 에는 없는 경로).
- **줍는 작업**을 이미 있는 운영 스크립트(`app/operations_cli.py` · `docs/OPERATIONS.md`)에
  얹는다. 새 구조를 만들지 않는다.
- ★ **처음에는 지우지 말고 세기만 한다.** 며칠 숫자를 보고 나서 삭제를 켠다.
  조건이 틀리면 **지우고 나서** 안다(§9).

---

## K-4. `album_bookmarks.album_id` 에 인덱스 — **크기: 작음**

`담아둔 앨범`은 `내 앨범`을 열 때마다 조회한다. 인덱스가 없다.

- FK 인덱스가 없는 컬럼이 16개인데 **나머지 15개는 죽은 테이블이거나
  `created_by`·`invited_by` 같은 저빈도**다. 이 하나만 넣는다.
- ★ **회귀 테스트를 만들지 않는다.**

---

## K-5. 남은 옛 설계를 기록만 한다 — **크기: 작음**

★ **고치지 않는다.** 전부 동작하는 코드다(§4 — 동작하는 코드를 다시 쓰지 않는다).
지금 걷으면 출시가 밀리고 새 결함이 난다. **시범운영에서 무엇을 실제로 쓰는지 본 뒤**가 싸다.

`docs/DB_NOTES.md` 를 만들어 아래를 적는다. 다음 사람이 "이거 왜 둘이지"에서
시간을 안 쓰게 하는 것이 목적이다.

### 같은 개념이 두 벌

| 옛 것 | 지금 쓰는 것 | 비고 |
| --- | --- | --- |
| `album_members` (참조 10곳) | `album_contributors` (26곳) | §1 이 "`album_members` 의 owner 행은 판정에 쓰지 않는다"고 이미 우회 중 |
| `album_media` (9곳) | `album_photos` (36곳) | |
| `families` · `family_members` · `family_invitations` | §1 의 역할 셋 | `authorization.py` 에 `family_role` 과 `album_role` 이 나란히 있다 |

★ **판정 근거가 여럿인 것이 이 제품의 반복 결함 원인**이다(H-1 · J-8 이 모두 여기서 났다).
새 기능을 얹을 때 **옛 쪽에 넣지 않는다.**

### 죽은 것으로 보이는 테이블

`guest_memory_submissions`(코드 참조 0) · `families`(0) ·
`memory_questions` · `memory_answers`(초기 질문 흐름의 잔재)

★ **지우지 않는다.** 지우려면 먼저 정말 안 쓰는지 확인해야 하고, 그건 지금 할 일이 아니다.

### 괜찮은 것 (확인함 2026-08-09)

- **RLS 가 전 테이블에 켜져 있고 대부분 정책이 0개** = 아무도 직접 못 읽는다.
  백엔드(service role)만 통과한다. §10 과 일치한다. **문이 하나다.**
- 핵심 일곱 테이블의 모델은 §7 의 세 계층을 그대로 담고 있다.
- 인덱스는 K-4 하나 말고는 문제 없다.

---

## 이 큐가 끝난 뒤 — J-QUEUE 로 돌아간다

```
J-10  내 앨범 썸네일이 느리다            보통
J-11  앨범 아래 빈 박스                  보통
J-12  🔴 참여자가 더한 것을 담는 규칙      큼
J-13  PDF 주소 www 제거                  작음
J-15  🔴 게스트 앨범 수명 (14일)          큼
```

★ **J-15 는 K-3 이 끝난 뒤에 한다.** 게스트 앨범을 지우려면 파일도 지워야 하는데,
그 방법이 K-3 에서 정해진다.
