# 미결 목록 (우리앨범)

> **이 파일이 유일한 기준이다.** 대화에서 말한 것은 여기 적히지 않으면 없는 것이다.
> 작업 커밋에 이 파일 갱신을 **함께 담는다.**
> 마지막 갱신: 2026-08-12 (사진 78장 전수 대조 + Cowork 직접 수정 반영)

---

## 0. 끝냈다 — 검증·커밋 대기 (Cowork 가 워크트리에서 직접 고침, `tsc` 통과)

| | 무엇 | 파일 |
| --- | --- | --- |
| ✔ R1 | **사진 준비 중 "빈 줄 두 개"** — 정체는 빈 카드가 아니라 **선 두 개**였다(준비 표시의 아래 테두리 + 푸터 위 테두리). 아래에 목록이 없으면 그 선을 긋지 않는다 | UploadForm.tsx · .css · preparingNoEmptyRows.test.ts |
| ✔ N7 | `참여 현황` 이 한 화면에 두 번 → 아래 줄을 `함께한 것` 으로 | CollaborationPanel.tsx |
| ✔ N8 | `✨` 이모지 제거 | CollaborationPanel.tsx |
| ✔ 신규 | **공유 실패 시 영어가 화면에 떴다** (`Kakao SDK is not ready.`) | AlbumShareSheet.tsx |
| ✔ 신규 | 카카오 카드 기본 문구에 옛말 `우리 모임 앨범이 완성됐어요` 가 남아 있었다 → 기본값 제거, 문구는 `CARD` 한 곳에서만 | useKakaoSdk.ts |
| ✔ 신규 | `userFacingError()` 도구 — 영어면 우리 말로 바꾸고, 우리말이면 그대로, 이유는 콘솔에 | lib/userFacingError.ts (+ 테스트) |

★ Claude Code 가 할 일: **전체 테스트(기준선 880)·빌드·커밋.** 코드를 더 고치지 않는다.

---

## 1. 코드에서 확인해 보니 **이미 고쳐져 있었다** (사진이 수정 전 것)

R3 미리보기 깨진 자리(`alt=""` + 회색 자리) · R4 오류 화면 영어(`albumTrouble.ts` 전부 우리말) ·
R5 대표사진 안내 문구(없음) · R6 대표사진 고른 표시(테두리 + `✓` 배지) ·
R8 더보기 연락처(`overflow-wrap: anywhere`) · N1 `window.prompt`(코드에 없음) ·
N2 로딩 분홍 상자(`loading-shimmer` 가 중립) · N3 버튼 두 줄(둘 다 `nowrap`) ·
N5 카톡 카드 옛 문구(`<title>` 과 OG 교체됨) · N6 `www` 주소(코드에 없음) · T1 `onTop`(제거됨)

---

## 2. 코드로는 판정 못 한다 — **브라우저에서 재야 한다**

| | 무엇 | 사진 |
| --- | --- | --- |
| R2 | 앨범 아래 **빈 테두리 상자**. `.album-page__manage:empty` 가드는 있는데 사진의 그 상자가 어느 요소인지 모른다 | 08-09 09:50 · 08-10 05:22 |
| R7 | 참여자 화면 제목 위 여백 | 08-09 08:51 |
| N4 | 계정 드롭다운이 제목 위에 겹친다 | 08-07 09:16 |
| V5 | 성공 토스트(#2f6b44)와 참여자 배지(#14514f) 혼동 | 참여 화면 |

★ **짐작으로 고치지 않는다.** 8월 10일에 그래서 K-18·J-11 을 못 고쳤다.

---

## 3. 🔴 큰 것 — 56곳이 서버가 준 말을 화면에 그대로 낸다

`X instanceof Error ? X.message : "…"` 형태가 **56곳**이다(SCREEN_SPEC §11 26차 위반).
백엔드는 대부분 우리말이라 당장 다 새는 것은 아니지만, **Supabase 인증·카카오 SDK 는 영어**다.

- 오늘 영어가 확실한 5곳만 `userFacingError()` 로 바꿨다.
- 나머지 **51곳은 테스트를 돌릴 수 있는 곳에서 한 번에** 바꾼다(기계적 치환 + 전체 테스트).

---

## 4. 커밋이 아예 없다 — 확실한 미결 넷

J-10 썸네일 느림 · J-12 🔴 참여자가 더한 것을 담는 규칙 · J-14 `Momento` 흔적 · J-15 🔴 주인 없는 앨범

---

## 4-1. 아이폰 (2026-08-13 · iOS 전수 조사에서 나온 것)

| 자리 | 상태 |
| --- | --- |
| 링크 복사 (`AlbumShareSheet`) | 고침 — `copyTextFromPromise`. 주소를 `await` 하기 전에 클립보드를 잡는다. |
| PDF 저장 (`exportPdf`) | 고침 — 아이폰이면 blob 대신 **올려 둔 서버 주소**로 보낸다. |

★ 앞서 제가 `아이폰 사파리는 a[download] 를 무시한다` 고 말한 것은 **과장이었다.**
  iOS 13+ 는 `download` 를 지원한다. 실제 코드도 주 경로가 서버 서명 URL(`?download=`)
  이라 아이폰에서도 받아진다. 다만 blob 경로는 기기·버전에 따라 파일 이름을 잃거나
  미리보기로 열고 끝나며 **예외가 없어** 화면에는 "저장했어요"만 남는다. 그래서
  주소가 있으면 그쪽으로 합류시켰다(새 경로를 만들지 않았다).

**남은 위험 — 코드로는 여기까지다. 기기에서 이렇게 확인한다.**

1. 아이폰 **사파리**로 `dev.woorialbum.com` → 앨범 → `PDF 저장`.
   - 기대: 주소창 옆 다운로드 표시가 뜨고, `파일` 앱 > `다운로드` 에 앨범 이름의 PDF 가 생긴다.
   - 실패 모양: 새 탭에 PDF 가 열리기만 하고 파일이 안 생긴다 / 이름이 `download` 다.
2. 아이폰 **카카오톡**에서 같은 것 — 예전 그대로여야 한다(이번 수정과 무관한 길).
3. 한 번도 저장한 적 없는 앨범으로 할 것. 이미 만든 PDF 가 있으면 1번 갈래(캐시)로 빠져
   이번에 고친 자리를 지나가지 않는다.
4. 1번이 실패하면 남은 길은 하나뿐이다 — 화면에 "다른 브라우저로 열기" 안내를 띄우는 것.
   그때 이 칸에 실패 화면을 붙인다.

---

## 4-2. 관리자 대시보드 10초 — 두 번 훑던 것을 한 번으로 (2026-08-13 · 고침)

질의가 느린 게 아니라 **저장소를 두 번 훑는다.** 경로가 `albums/<앨범>/photos/<사진>/`
이라 사진 하나가 폴더 하나고, `list_recursive` 는 폴더마다 LIST 를 한 번씩 보낸다.
`count_orphan_files` 가 `albums` 밑을, `storage_usage` 가 버킷 전체를 **각각** 훑는다.
사진 53장에 약 150회 순차 왕복 = 10.1초. 사진 500장이면 1000회를 넘는다.

고치는 법 (동작을 바꾸지 않는 쪽): 버킷 전체 훑기는 `albums` 밑을 포함하므로
**한 번만 훑고 두 계산이 그 목록을 나눠 쓴다.** 두 함수에 `files` 인자를 선택으로
받게 하고 `admin_service` 만 한 번 훑어 넘긴다. CLI(`operations_cli`) 는 그대로 둔다.
→ 왕복 절반. 그래도 사진 수에 비례하는 성질은 남는다. 근본은 저장소 훑기를
   대시보드에서 떼는 것(별도 버튼/주기 작업)이지만 MVP 에서는 안 한다.

**고침 — 커밋 `b95ff6d`.** 위 방법 그대로. `count_orphan_files` · `storage_usage` 가
`files` 를 선택으로 받고, `admin_service` 가 버킷을 한 번 훑어 둘에 넘긴다. 인자를 안
주면 지금까지처럼 스스로 훑으므로 `operations_cli` 는 한 줄도 안 바뀌었다.
`orphan_count` · `stored_count` · `bytes` · `file_count` 가 수정 전과 같은 값인지 검사로
고정했고, 대시보드를 한 번 그릴 때 `list_recursive` 가 **한 번만** 불리는지도 고정했다
(`tests/test_admin_dashboard_storage_scan.py`). 저장소가 막히면 0 으로 두고 대시보드는 뜬다.

★ **남는 한계는 그대로다** — LIST 횟수는 여전히 사진 수에 비례한다. 왕복이 절반이 됐을
   뿐이고, 사진이 크게 늘면 다시 느려진다. 근본 해법(대시보드에서 훑기를 떼는 것)은
   여전히 안 했다.

---

## 5. 오늘 dev 에 있으나 실기기 미확인

대표사진 저장 · 보조색 · `앨범 만들기` 한 줄 · 앨범 종류 여섯(`모임`) · 첫 화면 표지 띠 ·
`삭제` 글자 낮춤 · 되살리기 새로고침 · 초대장 세로 사진 비율

★ **운영 배포 순서**: 운영 DB 에 `album_category_gathering` 마이그레이션을 **먼저** 적용하고
  그 다음 main 머지. 거꾸로 하면 `모임` 을 고른 사람이 앨범을 못 만든다.
  (개발 DB 에는 2026-08-12 적용 완료)

---

## 6. PO 가 해야 할 것

P1 `ANTHROPIC_API_KEY` 폐기 · P2 Storage 고아 파일 · P3 루트 이미지 넷 판단 ·
P4 소개 페이지 "왜 만들었나" · P5 로그인 전 첫 화면 사진 · P6 아이콘 SVG ·
P7 카카오 캐시 초기화(`woorialbum.com` · `www.woorialbum.com`)

---

## 7. 큐 문서 정리 ★ 반복의 근본 원인

`K-QUEUE`(26) · `J-QUEUE`(17) = 43항목 중 **완료 표시가 붙은 것이 2개뿐**이었다.
실제 커밋은 39개. **끝난 것을 안 적어서 무엇이 남았는지 아무도 몰랐다.**
→ 각 항목에 완료 커밋 해시를 적어 넣는 작업이 필요하다.

---

## 규칙

1. 대화에서 "나중에" 라고 말한 것은 **그 자리에서** 이 파일에 적는다.
2. 작업 커밋에 이 파일 갱신을 함께 담는다.
3. 작업 시작 전에 0번과 2번부터 본다.
4. **코드로 판정 안 되는 것은 재고 나서 고친다.** 짐작으로 고치면 또 남는다.
