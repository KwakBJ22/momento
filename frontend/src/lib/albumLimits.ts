// 앨범 한도 상수 — 백엔드 app/models/schemas.py 의 DEFAULT_ALBUM_PHOTO_CAPACITY,
// DB(albums.photo_limit DEFAULT)와 같이 움직인다.

/** 앨범이 담을 수 있는 사진 총량. "한 번에 올리는 상한"(업로드 화면의 30장)과는
 *  별개다 — 30은 1회 업로드 성공률·40MB 가드 때문에 유지된다. */
export const ALBUM_PHOTO_CAPACITY = 100;

/**
 * PDF 저장을 허용하는 사진 수 상한.
 *
 * 근거(캔버스 한계 계산): exportPdf 는 html2canvas 가 앨범 전체를 캔버스 1장으로
 * 래스터화한 뒤 A4 높이로 잘라낸다. 호스트 폭 210mm ≈ 794px → A4 1페이지
 * ≈ 794 × 297/210 ≈ 1,123px(소스). Chrome 캔버스 최대 치수 65,535px, scale:2 이므로
 * 소스 높이 실질 한계는 32,767px ≈ 29페이지. 30장(약 32페이지)은 이미 한계에
 * 근접하지만 현재 프로덕션에서 동작이 확인된 최대 크기다. 그보다 큰 앨범은
 * 수학적으로 한계를 넘어 빈/잘린 PDF가 나오므로 누르기 전에 막는다.
 * 보수적 기준(실측 최대 = 30). 근본 해결은 200x200mm 판형 구현과 함께
 * exportPdf 를 구간 분할 렌더링으로 다시 쓸 때 처리한다 (KNOWN_ISSUES 참고).
 */
export const PDF_PHOTO_SAFE_LIMIT = 30;

/** 가드 발동 시 버튼 아래 표시할 문구 — 사실만 말한다(§8: "곧 지원" 약속 금지). */
export const PDF_BLOCKED_MESSAGE = "사진이 많은 앨범은 지금 PDF로 저장할 수 없어요.";

/** 더보기 시트의 차단 이유(4a): 숫자 표기가 아니라 사람 말로 먼저 답한다 —
 *  "100장 넣으라더니 30장까지냐"는 모순에 대한 설명. */
export const PDF_BLOCKED_REASON = `사진이 많으면 한 파일에 다 담기지 않아요. 한 번에 사진 ${PDF_PHOTO_SAFE_LIMIT}장까지 저장할 수 있어요.`;

/**
 * **이 기기가 못 만들 때**의 이유 (2026-08-21).
 *
 * 30장 상한은 크롬 기준으로 계산한 값이라(위 주석의 근거가 그렇다) 사파리에서는 그보다
 * 적은 사진에서도 한계를 넘는다. 그때는 장수가 아니라 **기기**가 이유다 — 같은 앨범이
 * 데스크톱에서는 만들어지므로, `사진이 많아서` 라고만 하면 거짓말이 된다.
 * ★ 위 문구와 같은 결로 쓴다: 사실 한 줄 + 무엇을 하면 되는지 한 줄.
 */
export const PDF_DEVICE_BLOCKED_REASON = "이 기기에서는 이 앨범을 한 파일로 만들기 어려워요. 컴퓨터에서 만들거나 사진을 줄여 주세요.";

/**
 * 캡션 글자 상한 — **종이에 다 나오는 만큼**이다 (PO 결정 2026-08-18).
 *
 * 정사각 판형(200×200mm)은 칸 높이가 고정이라 캡션이 **두 줄에서 잘린다**.
 * 캡션은 인쇄까지 가는 유일한 사용자 글이라, 반쪽만 남으면 그 사람이 남긴 말이
 * 아니게 된다. 그래서 자르는 대신 **처음부터 두 줄까지만 쓰게** 한다.
 *
 * 계산(가장 좁은 칸 기준 · PrintPages.css 의 사진 영역 174×154):
 *   4장 쪽 칸 폭 = (174 − 거터 6) ÷ 2 = 84mm = 317px
 *   캡션 9pt(12px) · 한글은 1em · 줄 끝이 남는 것을 감안해 92%만 찬다고 본다
 *   한 줄 ≈ 317 × 0.92 ÷ 12 ≈ 24자 → **두 줄 48자**
 *
 * ★ 가장 좁은 칸을 기준으로 잡는다. 1·2장 쪽(174mm)은 두 줄에 100자가 들어가지만,
 *   같은 사진이 어느 쪽에 놓일지는 장수에 따라 달라진다 — 넉넉한 쪽에 맞추면
 *   4장 쪽에서 잘린다.
 * ★ 배치 6종(다음 건)에서 칸 폭이 바뀌면 이 값도 같이 다시 잰다.
 * ★ 화면·서버가 같은 값을 쓴다 — backend/app/models/schemas.py 의
 *   `PhotoCaptionUpdate.caption` · `StoryPayload.text` 와 함께 움직인다.
 */
export const CAPTION_MAX_LENGTH = 48;

/** 상한에 가까워졌을 때만 남은 글자 수를 알린다 — 평소에는 아무 말도 하지 않는다. */
export const CAPTION_HINT_REMAINING = 10;
